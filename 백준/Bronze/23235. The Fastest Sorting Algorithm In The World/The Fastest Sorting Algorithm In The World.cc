#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N;
    int tc = 1;
    while (cin >> N) {
        if (N == 0) break;
        for (int i = 0; i < N; ++i) {
            int x; 
            cin >> x;
        }
        cout << "Case " << tc++ << ": Sorting... done!\n";
    }
    return 0;
}
